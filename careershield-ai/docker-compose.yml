services:
  careershield:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: careershield-ai
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - NODE_ENV=production
      # - GEMINI_API_KEY=your_gemini_api_key_here
    restart: unless-stopped
